from documents import views
from django.urls import path, re_path

urlpatterns = [
    # re_path(r'^valutes/$', views.StockListView.as_view(), name='valutes'),
    # re_path(r'^valutes/cash_edit/(?P<pk>\d+)$', views.StockEditView.as_view(),
    #         name='valute_edit'),
    # re_path(r'^valutes/stock_create/$', views.StockCreateView.as_view(),
    #         name='valute_create'),
    # re_path(r'^delete_cash/$', views.delete_stoks_view, name='delete_cash'),
]